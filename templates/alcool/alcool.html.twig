{% extends 'base.html.twig' %}
    
{% block body %}
    <h1>Alcool</h1>

    <button class="btn btn-outline-primary" onclick="rtn()">Retour</button>

    <br></br>
    

    {% for alcool in alcools %}
    <button type="button" class="btn btn-primary btn-lg"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;"><a 
    class="nav-link" href="/panier/add/{{ alcool.id }}">{{ alcool.title }}</a></font></font></button>
    {% endfor %}

    <script>
      function rtn() {
        window.history.back();
      }
    </script>

{#UPDATE `product` SET `category_id`= 1 WHERE category_id IS NULL#}
{% endblock %}    

